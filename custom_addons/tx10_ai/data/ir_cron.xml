<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="ir_cron_run_agent" model="ir.cron">
        <field name="name">TX10 AI: Run pending agent cycles</field>
        <field name="model_id" ref="model_tx10_ai_chat"/>
        <field name="state">code</field>
        <field name="code">model._cron_run_pending_chats()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
        <field name="priority">5</field>
    </record>

    <record id="ir_cron_classify_documents" model="ir.cron">
        <field name="name">TX10 AI: Classify pending documents</field>
        <field name="model_id" ref="solar_project.model_solar_document"/>
        <field name="state">code</field>
        <field name="code">model._cron_classify_pending_documents()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
        <field name="priority">10</field>
    </record>
</odoo>
